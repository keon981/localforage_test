const createImg = (n: number) => Array.from(new Array(n), (_, i) => i + 1)
export const image = createImg(100)

// const createSeries = (n: number) => Array.from(new Array(n), () => ({
//   ser:
// }))
